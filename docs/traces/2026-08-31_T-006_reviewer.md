## Trace

- 2026-08-31 — role: Reviewer / Tester
  - read: the diff — the shell, five components, the stylesheet, both pages, the chart module and
    its tests, and the new build integration in `astro.config.mjs`
  - checks: clean `npm ci` · unit 12/12 · isolation · typecheck 0 errors across both passes · build ·
    integration 3/3 · `harness-lint` clean at 648/650
  - probe 1: built, seeded, ran `wrangler dev` and fetched both pages. 200 each. The progress panel
    precedes the `outstanding` section in the DOM and projects are ordered recent-first, so `AC-G1`
    holds in the markup rather than in the styling.
  - probe 2: counted marks in the served HTML — 28 in the desktop chart, 14 in the mobile one, over
    different data because the `T-005` seed spans 27 days.
  - probe 3: motion worst case is 28 × 6ms + 220ms = 388ms, inside the 400ms threshold, and
    `prefers-reduced-motion` cancels it. Targets measure 58px mobile and 56px desktop.
  - probe 4: no red in any stylesheet, `faint` carries no text, and the three `backdrop-filter`
    lines are one `.glass-panel` rule — one surface, not the three the cap would have flagged.
    Checked before claiming.
  - probe 5: posted an entry and reloaded `/`. It appears. The loop is closed end to end.
  - probe 6, the owner's observation: zero Tailwind utility classes in any template, and zero uses
    of the `var(--color-*)` variables `@theme inline` generates, against 28 uses of `var(--page-*)`.
    Tailwind supplies only its reset. Recorded as Medium; the resolution is a decision, not a fix.
  - probe 7: the build fetches six files from Fontshare on every run and throws on failure, so a
    required gate now depends on the network. Mine — `design-handoff.md:22` told it to.
  - restored: worker stopped, database reset and reseeded.
  - blockers: none behavioural. One finding needs an owner decision before it can be closed.
