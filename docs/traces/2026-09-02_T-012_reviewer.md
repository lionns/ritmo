## Trace

- 2026-09-02 — role: Reviewer, round 1 on `T-012`
  - read: the diff, `T-012`, `US-1`/`US-2`, `FR-13`/`FR-15`/`FR-17`, `data-model.md` § Owner,
    § Area, § Project, the new `core/rules/project.ts`, the four API surfaces, § Setup and Capture
  - five gates re-run here: unit 33/33, isolation, typecheck 0 errors, Node build, integration 7/7,
    `harness-lint` clean
  - reviewed from an empty database rather than from the diff, since that is the promise: `db:reset`
    with no seed, built server on a throwaway `RITMO_DB_PATH`. The question carried no `value` and
    no numeric placeholder and named a bad week, a second setup returned 409, three projects against
    a cap of 2 gave active/active/shelved, the uncapped area stayed active, and an entry against an
    owner-created project moved the mark 9px `empty` → 104px `timed`
  - checked the copy against `US-2` and `NFR-7` by reading the rendered page, not the source: a
    count and a plain `ARCHIVADOS` section, no softening, congratulation or apology
  - did not take the cap rule on its tests' word: mutated `>=` to `>`, then dropped the
    `countsAgainstCap` term, and each mutation failed exactly one test. Restored after
  - probed the `FR-14` assumption by inserting a closed week by hand. The state-change guard fires
    (422), but creation and cap raises do not consult it — with that week present, raising the cap
    and creating a project returned it `active` mid-week. Two Medium from that and its sibling, both
    latent because nothing in the product closes a week yet; routed to `/semana`, not to Codex
  - fixed nothing. Throwaway database and the hand-inserted week deleted; owner's file untouched
