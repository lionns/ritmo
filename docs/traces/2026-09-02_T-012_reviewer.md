## Trace

- 2026-09-02 — role: Reviewer, rounds 1-2 on `T-012` (compressed)
  - five gates re-run each round; round 2 green at unit 33/33 and integration 7/7
  - reviewed from an empty database rather than from the diff, since that is the promise: `db:reset`
    with no seed, built server on a throwaway `RITMO_DB_PATH`. The question carried no `value`, a
    second setup returned 409, three projects against a cap of 2 gave active/active/shelved, the
    uncapped area stayed active, and an entry moved the mark 9px `empty` → 104px `timed`
  - did not take the cap rule on its tests' word: mutated `>=` to `>`, then dropped
    `countsAgainstCap`; each failed exactly one test. Probed the `FR-14` assumption by inserting a
    closed week by hand — the state-change guard fires, creation and cap raises do not consult it.
    Two Medium from that, both latent, both routed to `/semana`. Probes and week row deleted

- 2026-09-02 — role: Reviewer on owner validation, then UX, then close
  - the owner used it for real and found two things no gate could: the settings panel grew with each
    area until it left the stage, and the first-run headline was unreadable
  - confirmed the first in the code, not from the screenshot — three of four panels lacked the bound
    § Responsive Behavior already required. The second was mine: § Setup and Capture said the
    headline *is* the question and Codex built exactly that, but sixty characters cannot fit a
    `clamp(76px,9.2vw,132px)` face. Corrected the section for both, with reasoning and date
  - round 2: both closed, and Codex took the better shape — the bound moved into `PageStage` as one
    rule instead of three per panel. Verified structurally, parsing the rendered `<main>` on all
    three routes; the compiled rule sits in `@media (width>=75rem)`, matching `--breakpoint-xl`.
    Tests moved with it. Nit: that selector is positional
  - closed on the owner's validation, final checks re-run; throwaway database deleted
