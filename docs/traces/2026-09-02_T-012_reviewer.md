## Trace

- 2026-09-02 — role: Reviewer, round 1 on `T-012` (compressed)
  - five gates re-run: unit 33/33, isolation, typecheck 0 errors, build, integration 7/7, lint clean
  - reviewed from an empty database rather than from the diff, since that is the promise: `db:reset`
    with no seed, built server on a throwaway `RITMO_DB_PATH`. The question carried no `value` and
    named a bad week, a second setup returned 409, three projects against a cap of 2 gave
    active/active/shelved, the uncapped area stayed active, and an entry against an owner-created
    project moved the mark 9px `empty` → 104px `timed`. Copy read from the rendered page, not the
    source: a count and a plain `ARCHIVADOS` section, no softening or apology
  - did not take the cap rule on its tests' word: mutated `>=` to `>`, then dropped
    `countsAgainstCap`; each failed exactly one test. Restored after
  - probed the `FR-14` assumption by inserting a closed week by hand. The state-change guard fires,
    but creation and cap raises do not consult it — two Medium, both latent because nothing closes
    a week yet, both routed to `/semana`. Fixed nothing; probes and week row deleted

- 2026-09-02 — role: Reviewer on owner validation, then UX
  - the owner used it for real and found two things no gate could: the settings panel grows with
    each area until it leaves the stage, and the first-run headline is unreadable
  - confirmed the first in the code, not from the screenshot: `PortfolioPanel` carries the bound,
    `SettingsPanel` none, and § Responsive Behavior already required it. Three of four panels lack it
  - the second is mine: § Setup and Capture said the headline *is* the question and Codex built
    exactly that, but sixty characters cannot fit a `clamp(76px,9.2vw,132px)` face. Owner settled
    “Una semana mala.” with the question below; `US-1` holds because the pair is the question
  - corrected § Setup and Capture for both, with reasoning and date; left `src/` to the implementer
