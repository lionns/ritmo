## Trace

- 2026-09-01 — role: Reviewer, `/code-review high` on the T-007 working-tree diff
  - read: the diff across contracts, ports, rules, adapter, API, seed and four test files; `T-007`
    plan; `data-model.md` § NextAction and § Invariants; `migrations/0001_initial_schema.sql`
  - retrieved: Cloudflare D1 `batch()` transaction semantics from the Workers API docs, rather than
    recalling them — the finding on the zero-row UPDATE turns on exactly that contract
  - checks: unit 23/23, integration 4/4, isolation and harness lint green, run here not read
  - probes: `db:reset` then `seed` twice — all three active projects printed one open action each
    time, so the invariant query is real and the seed is idempotent
  - found: 6 — one Medium (`replaceNextAction` drops the row count and binds the replacement's
    identity, so it can leave two open actions the partial index cannot catch), five Low
  - cleared: batch atomicity and ordering, double-count under the partial index, `?? 0` for a
    project with no plan, and the ISO comparison given clock-assigned `occurredAt`
  - not fixed: findings belong to the Backend Implementer. This is the first review in the project
    where author and judge are genuinely different agents, and writing the fixes here would spend
    exactly what `agent-config.md` set the split up to buy
  - blocker: `T-007` stays `review`; owner validation follows the fixes
