## Trace

- 2026-09-01 — role: Reviewer, rounds 1-2 on `T-008`
  - read: the diff, `T-008`, § The Hero Chart and § The Log Form, `hero-chart.ts`, `entry-form.ts`,
    `api/entries.ts`, `seed-local.mjs`. Five gates re-run each round
  - two Medium, two Low, three nits; all four closed by round 3. The first was computed, not
    eyeballed: the client's fixed 90-minute denominator against `hero-chart.ts:65`'s window maximum,
    on the seed data the implementer's own probe used — 58px animated, 95px on reload, and 95 is the
    number the Outcome recorded as a success, because that probe read SSR only
  - not a finding: the stale per-mark `<title>`, already settled by § The Hero Chart. One Low was
    mine — "the handoff wording is the owner's" meant only the colour sentence
  - owner question answered with evidence: nothing under the form caps at 90
    (`0001_initial_schema.sql:114`), the day accumulates (`store.ts:274` has no `LIMIT`), so a split
    session corrupts the entry count `progressSincePlan` feeds, not the chart. Owner settled
    `15 / 30 / 60 / 120`; I wrote the handoff, plan and records, and left `src/` to Codex

- 2026-09-01 — role: Reviewer, round 3, then Planner
  - checks re-run here: unit 29/29, isolation, lint, typecheck, build, integration 4/4
  - verified the floor in the build, not the source: client minifies to `Math.max(12,e)`, and
    `buildChartDays` does not follow the new cross-import into it. The re-probe checks out
    arithmetically: seed's tallest day is 50, a 60-minute entry makes 60 the maximum, so 104px
  - new Medium, mutation-probed both ways: `src/lib/__probe.ts` importing an adapter passes
    `check:core`; the same import under `src/components/` fails. `isFront` mirrors `D-009`, which
    names only routes and components, so the gap is the decision's first. Probes deleted
  - planned `T-009` rather than editing `scripts/` here; recommended `T-008` for owner validation
