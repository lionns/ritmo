## Trace

- 2026-09-01 — role: Reviewer, rounds 1-3 on `T-008` (compressed)
  - read: the diff, `T-008`, § The Hero Chart and § The Log Form, `hero-chart.ts`, `entry-form.ts`,
    `api/entries.ts`, `seed-local.mjs`. Five gates re-run every round, green each time
  - two Medium, two Low, three nits, all closed. The first was computed, not eyeballed: the client's
    fixed 90-minute denominator against `hero-chart.ts:65`'s window maximum, on the implementer's
    own seed data — 58px animated, 95px on reload, and 95 is the number its Outcome recorded as a
    success, because that probe read SSR only. One Low was mine
  - owner's over-90-minutes question answered with evidence; reasoning kept in § The Log Form
  - new Medium, mutation-probed both ways: a `src/lib/` file importing an adapter passes
    `check:core`, the same import under `src/components/` fails. Planned `T-009`
  - the accent criterion could not be met by anything — the save button alone is 5.4% of a 390px
    screen. Rewritten to § Navigation Map's count. Got one wrong: called `AC-2/5/9` fabricated when
    they are `T-006`'s criteria by position

- 2026-09-01 — role: Reviewer, round 4
  - five gates re-run, green (unit 29/29, integration 4/4). Chip matches the spec; `bg-accent`
    appears once in the `/registrar` tree. Checked the weight step was not decorative:
    `AppShell.astro:31` loads Martian Mono at exactly 300 and 400, which is why the resting chip had
    to drop rather than the selected one rise
  - three imprecisions were mine, fixed here: the handoff never named the weights or the fallback,
    and my criterion said "element" where the veiled marks are accent-filled too
  - closed on the owner's validation, final checks re-run at close. The 390px stopwatch and the
    `NFR-7` copy check are the owner's assertion, not numbers measured here
