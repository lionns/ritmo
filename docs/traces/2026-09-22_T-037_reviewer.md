## Trace

- 2026-09-22 — role: Reviewer, on work Codex wrote
  - read: `adapters/sqlite/export.ts`, `src/pages/api/export.ts`, the `/ajustes` diff, the
    integration suite, the backend trace, and `T-039` for whether it covers the new route
  - re-ran: all five gates — unit 55/55, isolation, typecheck 0 errors (3 pre-existing hints),
    build, integration 30/30, harness-lint clean
  - verified rather than accepted: exported a **copy** of the owner's database through the
    adapter and compared all 11 tables row by row — identical; valid SQLite header; integrity
    check ok; declared `Content-Length` equal to bytes received; source byte-identical after
  - started the built server on a throwaway copy and downloaded over HTTP: 200, 176128 bytes
  - probed the temp copy's lifetime: three aborted downloads over HTTP leave nothing behind,
    but three `exportDatabase()` calls whose streams are never read leave three full plaintext
    copies in `$TMPDIR`. Not reachable through the route today.
  - probed the missing-source path: throws, creates no file
  - findings: one Medium (`/api/export` returns the whole database unauthenticated and `T-039`
    did not name it — not live under `D-020`, decisive under `T-040`), two Low
  - acted: added two acceptance criteria to `T-039` — the route by name, and a deliberate
    decision about the `credentials` table travelling inside a plain export
  - credited: the implementer checked SQLite's backup semantics against `sqlite.org` instead of
    recalling them, which is the exact failure this project was bitten by before
  - result: approved, pending the owner's validation.
