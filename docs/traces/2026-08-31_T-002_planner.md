## Trace

- 2026-08-31 — role: Planner
  - read: `T-001` § Review, `PROTOCOLS.md` § Budgets and § Review, `VERSION.md` § Change Rules,
    `harness.json`, `scripts/harness-lint.mjs`
  - did: planned the budget split proposed in `D-015` after reviewing `T-001` hit the ceiling —
    four findings fit only by compressing a plan that was already agreed
  - files: `docs/tasks/T-002-split-task-budget.md`, `docs/decisions/D-015-...md`
  - checks: `harness-lint` clean apart from this task's own trace requirement, now satisfied
  - assumptions: `## Outcome` exists in every task written from the template, so it can serve as
    the split point without adding a field
  - blockers: `D-015` is `proposed`. Governance changes need explicit human approval
    (`VERSION.md` § Change Rules), so this task stays `blocked` until the owner accepts or rejects.
  - decisions: `D-015` proposed, not applied
