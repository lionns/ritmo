## Trace

- 2026-09-22 — role: Reviewer, on work Codex wrote
  - read: the diff in full, `0005_drop_next_actions.sql`, `D-028`, the backend trace, the
    integration test, `data-model.md` § Retired: NextAction, `D-024` for what it actually cites
  - re-ran: all five gates — unit 55/55, isolation clean, typecheck 0 errors (3 pre-existing
    hints), build, integration 26/26, harness-lint clean
  - verified rather than accepted: opened `data/ritmo.sqlite` and the pre-drop backup read-only
    and compared all ten surviving tables row by row — identical; `PRAGMA foreign_key_check`
    empty; the ledger holds one 0005 row; `tags` present and unchanged
  - verified the blocker was real: 5 next actions in the backup, the 4 open ones each with a
    matching step, 1 closed with none — exactly what the implementer reported
  - findings: one Medium (migrations apply on `openDatabase()`, so the copy-first control this
    task specified cannot hold while the app runs — it fired on the live database before the
    probe; T-040 moves months of records under the same procedure), three Low
  - the fourth acceptance criterion cannot pass as written, and I wrote it: historical migrations
    must keep executable `next_actions` SQL. Codex left it unchecked with a reason instead of
    rewriting history to satisfy it, which is the right call.
  - held back: no code changed during review. The Medium belongs to T-040 and T-033.
  - result: approved, pending the owner's validation.
