## Trace

- 2026-09-22 — role: Planner
  - read: `D-020` in full for the two clauses it arms, `D-005`, `NFR-4` as written,
    `astro.config.mjs`, `package.json`, and which decision settles each foundation topic
  - verified against live documentation, not recalled: `docs.turso.tech/sdk/ts/reference` lists
    Cloudflare Workers among compatible runtimes and documents interactive transactions and batch;
    `@tursodatabase/serverless` is the docs' edge recommendation and its own announcement calls it
    "currently experimental and subject to change"; `@libsql/client/web` "does not support local
    file URLs". The maturity question is handed to `T-038` rather than settled on a blog post.
  - found: **the task was scoped as one decision and is four.** Cloudflare plus Turso supersedes
    `deploy`, `data`, `runtime` and `interface`, and the linter permits one accepted decision per
    topic. I planned it without checking which foundations the move touched.
  - found: `FR-21` breaks on arrival — `T-037`'s export reads a local file through `node:sqlite`,
    which Turso has no equivalent of. It is the only thing behind `NFR-4`'s no-lock-in promise.
    Added to `T-038` as a criterion, per `D-029` §2.
  - corrected myself: `D-031` first said Cloudflare holds the keys and can read the data, carried
    over from `NFR-4`'s old wording, one paragraph after sending the data to Turso. Fixed before
    committing.
  - did: `D-031`…`D-034`; `D-020`, `D-019`, `D-018`, `D-021` marked superseded; `NFR-4` rewritten
    (it still cited `D-005`, superseded on 2026-09-02); `T-038` given the package choice and the
    export rebuild
  - checks: harness lint clean — one accepted decision per foundation topic; no code touched
  - result: closable. `T-039` and `T-040` are unblocked; `T-040` still waits on `T-039`.
