## Trace

- 2026-09-02 — role: Reviewer, rounds 1-2 on `T-014`, then UX (compressed)
  - round 1 traced the clipped “Crear área” rather than guessing: `xl:auto-rows-[minmax(0,1fr)]`
    plus the headline's `row-span-2` and both panels' `col-start-2` sized each card to half the
    viewport, and `max-height:100%` cut the form through its own submit button. The worse finding
    was the one the owner felt: two independent scroll surfaces stacked in one column
  - round 2's fix was clean and still wrong. Measured: the headline column took 787px — 60% of the
    width — to hold a title. **Both rounds were the plan's fault, and all handbacks mine**: it asked
    for two surfaces and for every panel to be bounded without saying how they share the height
  - owner settled full width. § Setup and Capture carries the rule it leaves: data-entry routes get
    the width, reading routes get the stage

- 2026-09-03 — role: Reviewer, round 3, **first round with a browser**
  - Chrome integration works. Measured instead of reading CSS: cards 624px each, nothing clipped
    (`scrollHeight == clientHeight`), headline at Title — the spec's shape is met
  - and found what three rounds of source-grep could not: Áreas 465px against Proyectos 1091px, a
    627px void, and 635px of page scroll. Cause: 624px cards with single-column fields, so
    `Minutos estimados` is a 566px input — the width went into length instead of height
  - **caught before handing back**, which is the whole reason the integration earns its place
  - my "short enough that the page does not scroll at all" was written without measuring and was
    wrong; § Setup and Capture now carries the measured numbers and says so
  - owner added two findings, both unwritten rules: the disclosure `+` never changes, and no control
    sets `appearance` so OS chrome leaks. Wrote both into § Interaction States rather than fixing
    two instances — an unwritten rule gets invented again on the next screen
