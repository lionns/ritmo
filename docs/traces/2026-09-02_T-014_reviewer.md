## Trace

- 2026-09-02 — role: Reviewer, rounds 1-2 on `T-014`, then UX (compressed)
  - round 1 traced the clipped “Crear área” rather than guessing: equal grid rows sized each card to
    half the viewport and `max-height:100%` cut the form through its own submit button. The worse
    finding was the one the owner felt — two scroll surfaces stacked in one column
  - round 2's fix was clean and still wrong. Measured: the headline column took 787px, 60% of the
    width, to hold a title. **All three handbacks were the plan's**, which asked for two surfaces
    and for every panel to be bounded without saying how they share the height
  - owner settled full width; § Setup and Capture carries the rule: data-entry routes get the width

- 2026-09-03 — role: Reviewer, rounds 3-4, **first rounds with a browser**
  - Chrome integration works. Measured instead of reading CSS, and found what three rounds of
    source-grep could not: Áreas 465px against Proyectos 1091px, a 627px void, 635px of page scroll.
    Cause: 624px cards with single-column fields, so `Minutos estimados` was a 566px input — the
    width went into length instead of height. **Caught before handing back**, which is the point
  - my "short enough that the page does not scroll at all" was written without measuring and was
    wrong; § Setup and Capture now carries the measured numbers and says so
  - owner added two findings, both unwritten rules — a `+` that never changes, and OS chrome leaking
    through unset `appearance`. Wrote both into § Interaction States rather than fixing two instances
  - round 4: Áreas 554px against Proyectos 651px, a **97px** difference, better than the ~218px I
    predicted. Fields pair at 275px, nothing clipped, chevron on the `dim` token. Closed on the
    owner's validation, after four rounds and three of them mine
  - stopped growing the task instead of taking a fifth round: form errors and the native dropdown
    span three screens, so they are `T-015`, planned with a browser in its verification
