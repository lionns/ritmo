## Trace

- 2026-08-31 — role: Reviewer / Tester, rounds 1 and 2 (compressed)
  - probed the split rather than reading it: both budgets fire at exactly 121 and 61 naming their
    own budget · `plan + record` equals the file total on every task · a fenced `## Outcome`
    mis-splits, loudly. Round 1 found that deleting `taskPlanLines` let a 280-line plan report clean
    and that `journalEntryLines` was read by nothing — both became `D-016`. Round 2 confirmed the
    contract fires in both directions, but its enforced set was a hand-written list, so the same
    defect stayed reachable for any budget added later.

- 2026-08-31 — role: Reviewer / Tester, round 3
  - read: the diff (4 files), the implementer trace, `D-016`
  - checks: clean `npm ci` · unit 7/7 · isolation · typecheck 0 errors over 17 files · build ·
    integration 1/1 · `harness-lint` clean at 648/650
  - probe 1: re-ran the exact case that reported clean last round — a check reading
    `budgets.exampleLines` with the key undeclared. It now fails: `budget contract missing
    \`exampleLines\``. The Medium is closed against the evidence that opened it.
  - probe 2, first trap: added a reader to `harness-status.mjs`. Caught. The scan does not assume
    `harness-lint.mjs` is the only reader.
  - probe 3, second trap: `enforcedBudgetKeys()` returns exactly the five real keys, so the scanner
    does not match the regex literal living in its own source. Their test pins this.
  - probe 4: a comment reading `budgets.fantasmaLines` makes the linter demand that key. The scan is
    textual. Recorded as Low with a recommendation not to fix — it errs toward more enforcement.
  - blockers: none. Recommended for owner validation.
