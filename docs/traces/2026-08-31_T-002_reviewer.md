## Trace

- 2026-08-31 — role: Reviewer / Tester, round 1 (compressed)
  - probed the split rather than reading it: both budgets fire at exactly 121 and 61 naming their
    own budget · `plan + record` equals the file total on all three tasks · a task with no
    `## Outcome` is entirely plan · a fenced `## Outcome` mis-splits, loudly · deleting
    `taskPlanLines` let a 280-line plan report clean, and `journalEntryLines` was read by nothing.
    Those last two became `D-016`.

- 2026-08-31 — role: Reviewer / Tester, round 2
  - read: the diff (8 files), `D-016`, the implementer trace, `VERSION.md`
  - checks: clean `npm ci` · unit 6/6 · isolation · typecheck 0 errors over 17 files · build ·
    integration 1/1 · `harness-lint` clean at 648/650
  - probe 1: deleted `taskPlanLines` — `budget contract missing \`taskPlanLines\`, which harness-lint
    enforces`. Added `inventadoLines` — `budget contract declares \`inventadoLines\`, which
    harness-lint does not enforce`. Both directions fire and name the key.
  - probe 2: added a check reading `budgets.exampleLines`, left the key undeclared, and ran the
    linter. It reported clean while the new check silently no-opped, because `ENFORCED_BUDGETS` is
    hand-written and does not know the name. The defect `D-016` removes is still reachable for any
    budget added from here on. Recorded as the round-two Medium.
  - probe 3: `journalEntryLines` is gone and the one-line rule still holds by construction.
  - fixed in review: `T-001` and `T-002` had lost the blank line before `## Validation`. My own
    round-one edit script dropped it; `T-003` was never touched by it and was intact.
  - note: `docs/sdd` is at 648 of 650. The next governance prose change has two lines.
  - blockers: none — no finding breaks an acceptance criterion
