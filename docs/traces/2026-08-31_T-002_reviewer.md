## Trace

- 2026-08-31 — role: Reviewer / Tester
  - read: the diff (8 files plus a new test and trace), `D-015`, `VERSION.md` § Change Rules,
    `harness.json`, `scripts/lib/harness.mjs`, `scripts/harness-lint.mjs`
  - checks: clean `npm ci` · unit 5/5 · isolation clean · typecheck 0 errors over 17 files · build ·
    integration 1/1 · `harness-lint` clean at 643/650
  - probe 1: padded a plan to exactly 121 lines — `plan is 121 lines, plan budget is 120`. Padded a
    record to exactly 61 — `record is 61 lines, record budget is 60`. Both thresholds are off-by-one
    correct and each names its own budget.
  - probe 2: `plan + record` equals the file total on all three tasks (116, 75, 97). The split
    neither loses a line at the boundary nor counts one twice.
  - probe 3: deleted `taskPlanLines` from `harness.json` and gave T-003 a 280-line plan.
    `harness-lint` reported clean. A missing key disables its own check, and this task is the rename
    that makes a stale config possible. Recorded as the one finding worth fixing before closure.
  - probe 4: a `## Outcome` inside a fenced code block splits the file there — plan 15, record 89.
    It fails loudly on the record budget rather than passing silently, so it is noise, not a hole.
  - probe 5: no `taskFileLines` reference survives in `scripts/`; the two remaining mentions are
    prose that deliberately names the old key.
  - note, not a finding: `T-003` still declares `harness: 0.7.1` and will run under `0.8.0`. It
    records the version it ran under, so it bumps when work starts, not now.
  - blockers: none — no finding breaks an acceptance criterion
