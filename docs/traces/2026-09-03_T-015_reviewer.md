## Trace

- 2026-09-03 — role: Reviewer, round 1 on `T-015`
  - read: the diff, `T-015`, § Interaction States' Error and control-chrome rules, § Accessibility
    Notes, the new `FieldError`, `ListboxField`, `form-errors.ts` and `listbox.ts`
  - five gates re-run: unit 49/49, isolation, typecheck 0, build, integration 7/7, lint clean
  - **first task whose Verification assumes a browser, and it was used**: options measured 56px at
    desktop and 58px at 390px, trigger 58px; `aria-invalid`, `aria-describedby`, `role="listbox"`,
    `role="option"`, `aria-activedescendant` and `aria-selected` all present; errors use three
    channels — dashed border, `ink`, `aria-hidden` arrow — so nothing is marked by colour alone
  - nearly filed a false finding: five error elements read as bare `↑` through `textContent`. They
    are `hidden` at zero height. Checked before writing it down
  - **probed rather than accused**: the listbox would not open with `Enter`, and the alternative
    explanation was my own key dispatch. Attached a listener — the keydown reaches the button
    (`["Enter"]`) and `click` stays at **0**, with `aria-expanded` false. A native button fires click
    on Enter, so the component consumes the key while closed. `Space` and `ArrowDown` the same
  - owner's finding traced to `align-items: flex-end` on a row whose cells are asymmetric: the name
    cell grows 56 → 114px with the message and the label-less checkbox slides 29px. Confirmed by
    contrast against the project form, whose pairs move together because both cells have labels
  - fixed nothing; both Medium returned to the implementer with the causes rather than the symptoms
