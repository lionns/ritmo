## Trace

- 2026-09-03 — role: Reviewer, round 1 on `T-015` (compressed)
  - five gates re-run: unit 49/49, isolation, typecheck 0, build, integration 7/7, lint clean
  - **first task whose Verification assumed a browser, and it was used**: options 56px desktop and
    58px at 390px; `aria-invalid`, `aria-describedby`, `role="listbox"`, `aria-activedescendant` and
    `aria-selected` all present; errors use three channels — dashed border, `ink`, `aria-hidden`
    arrow — so nothing is marked by colour alone
  - nearly filed a false finding: five error elements read as bare `↑` through `textContent`, and
    are `hidden` at zero height. Checked before writing it down
  - **probed rather than accused**: the listbox would not open with `Enter`, and the alternative was
    my own key dispatch. A listener showed the keydown reaching the button and `click` at **0**
  - owner's finding traced to `align-items: flex-end` on asymmetric cells — the label-less checkbox
    slid 29px. Confirmed by contrast against the project form, whose pairs move together

- 2026-09-03 — role: Reviewer, round 2
  - both Medium closed, each verified by the probe that found it. Opening now shows
    `btn:keydown(Enter)` → **`btn:click`** — zero in round 1 — → open → focus to listbox. Walked the
    rest: arrows move, `End` jumps, `Enter` commits and returns focus, `Escape` closes uncommitted
  - the Áreas row holds at 0px where round 1 measured 29, via `subgrid` plus an `aria-hidden` label
    spacer; confirmed `subgrid` support rather than assuming it. Created a project through the form
    with the area chosen by keyboard, and it lands with its action
  - **method caveat**: several key presses produced no events until a real mouse click established
    activation. Read naively that looks like a dead control, so every keyboard verdict here is
    quoted with its event log rather than on its own
