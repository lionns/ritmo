## Trace

- 2026-09-23 — role: Reviewer, on work Codex wrote — round 1
  - re-ran the gates instead of reading them off the trace: unit, isolation, types and both builds
    green, but `npm run test:integration` **39 failed of 76** — `sqld` absent here and both libSQL
    suites failing hard rather than skipping, so the remote adapter and the rebuilt export were
    unverifiable off the machine that downloaded the binary
  - returned under `D-029` §1. Nothing committed — committing a red gate contradicts the gate.

- round 2
  - integration now 37 passed / 39 skipped, **exit 0**, naming what is not verified; probed the
    hatch — `RITMO_SQLD_BINARY=/nope/sqld` exits **1**, so it cannot be abused
  - `docs/development.md` carries the dependency rationale and the implementer did not
    self-approve it. Wrote `D-036` as Planner; `D-022` superseded.
  - a Medium accepted without a new task: the remote export caps SQL input at 16 MiB and fails
    explicitly above it. 176 KB today; recorded in `D-036`.

- round 3 — closing the open item
  - installed `sqld` 0.24.32 from the official release, published SHA-256 matched: gate **76/76**
  - went past their suite — ran the **real app** on a disposable remote store, created an owner,
    area, project, step and entry through the real HTTP API, downloaded `/api/export` and opened
    it: 11 tables, integrity ok, foreign keys clean, six migrations in the ledger. Repeated on
    **workerd**, the runtime that deploys: served, wrote, exported, valid.
  - cleaned up: servers stopped, `.dev.vars` removed, owner's database untouched
  - result: approved and closed; nothing new found in round 3.
