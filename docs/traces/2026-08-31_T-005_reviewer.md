## Trace

- 2026-08-31 — role: Reviewer / Tester
  - read: the diff — contracts, two rules, the D1 methods, both endpoints, the seed, the tests
  - checks: clean `npm ci` · unit 9/9 · isolation · typecheck 0 errors across both passes · build ·
    integration 3/3 · `harness-lint` clean at 648/650
  - probe 1: built, ran `wrangler dev`, and drove the real worker. `GET /api/portfolio` 200 with
    three projects, their areas, entries and next actions · `POST /api/entries` 201 · the following
    `GET` shows the new entry with `effortMinutes` and `note` both `null` · a missing project and a
    shelved one each return 422 naming the project. The loop works end to end.
  - probe 2: closed the open next action of one active project in SQL — the ordinary result of
    finishing it — and called the endpoint again. **HTTP 500, `Portfolio could not be read`.** The
    whole surface, not one card. `readPortfolio` throws when an active project has no open action,
    and nothing at write time requires a replacement. Recorded as High.
  - probe 3: `npm run seed` twice from empty, both green, and it prints that the seeded owner stands
    in for authentication and this build must not be deployed. But no seeded project lands in
    `outstanding`, so that branch never appears on screen. Recorded as Low.
  - probe 4: entries and open next actions are each read in one query; areas are one query per area.
    Recorded as Low against the 10 ms budget of `D-001`.
  - restored: database reset and reseeded, worker stopped, tree clean.
  - blockers: the High blocks `T-006`, which consumes this contract. Nothing blocks closing it once
    `nextAction` admits null.
