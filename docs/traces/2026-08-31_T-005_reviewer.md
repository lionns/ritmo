## Trace

- 2026-08-31 — role: Reviewer / Tester, round 1 (compressed)
  - built, ran `wrangler dev` and drove the real endpoints: 200, 201, the entry appearing on the
    next read, and 422 naming the project for both rejection paths. Found one High — closing an
    open next action returned 500 for the whole portfolio, not one card — and three Low: areas read
    one query each, no seeded project in `outstanding`, and `getOwner()` without an id.

- 2026-08-31 — role: Reviewer / Tester, round 2
  - read: the diff — contract, rule, port, adapter, both endpoints, the seed, the tests
  - checks: clean `npm ci` · unit 9/9 · isolation · typecheck 0 errors across both passes · build ·
    integration 3/3 · `harness-lint` clean at 648/650
  - probe 1: re-ran the probe that opened the High. Closed an open next action on the running
    worker; `GET /api/portfolio` now returns **200** with that project at `nextAction: null`, the
    others untouched, and `POST /api/entries` still 201. It was 500 for the whole surface.
  - probe 2: the seeded portfolio now serves all three states the front has to build — a project
    with an action, one with `nextAction: null`, and one in `outstanding` with no recent entries.
  - probe 3: `readAreas` is one `IN (…)` query; `LOCAL_OWNER_ID` lives in `adapters/local-owner.ts`
    and is imported by the seed, the tests and both endpoints, so nothing can drift.
  - found: `RECENT_PROGRESS_DAYS = 14` cannot feed the 28-mark desktop chart that
    `design-handoff.md:129` and `T-006` both require. My planning error — the assumption I wrote
    named the mobile figure and I never checked it against the desktop one in the same file.
    Recorded as Medium and the assumption corrected.
  - restored: worker stopped, database reset and reseeded.
  - blockers: the Medium blocks `T-006` for the same reason the High did.
