## Trace

- 2026-08-31 — role: Reviewer / Tester
  - read: the diff (8 files plus `tsconfig.core.json` and a trace), `D-004`, `D-009`, the `T-001`
    reviewer trace that recorded the six findings
  - checks: clean `npm ci` · unit 7/7 · isolation · typecheck 0 errors, now with a second core-only
    pass · build · integration 2/2 · `harness-lint` clean at 648/650
  - probe 1: after `npm run db:reset`, a duplicate `(owner_id, starts_on)` fails `UNIQUE constraint
    failed` and an orphan owner fails `FOREIGN KEY constraint failed`. Removing the `PRAGMA` cost
    nothing, which is what its removal claimed.
  - probe 2: the build log has no `Enabling sessions`, and `dist/server/wrangler.json` carries
    `kv_namespaces: []` and `assets.directory: "../client"`. The double-nesting risk I recorded
    against this change did not materialise.
  - probe 3: `Env` and `D1Database` in a core file now fail `tsconfig.core.json` with `TS2304`.
    But `Response`, `crypto` and `fetch` still pass, because `"types": ["node"]` declares them.
    `"types": []` compiles core clean and catches them. Recorded as Low.
  - probe 4: the deprecation the new comment cites is real —
    `@cloudflare/vitest-pool-workers/types/cloudflare-test.d.ts:3`.
  - probe 5: reverted `setup.ts` to its old top-level form. The second test then fails on `UNIQUE
    constraint failed: owners.id`. The rewrite fixed a latent bug that was in nobody's findings; the
    suite had passed only because it held one test.
  - blockers: none — no finding breaks an acceptance criterion
