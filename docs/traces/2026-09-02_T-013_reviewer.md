## Trace

- 2026-09-02 — role: Reviewer on `T-013`
  - read: the diff, `T-013`, `FR-6`, `data-model.md` § NextAction and its invariant list, § The
    Project Row, the existing `core/rules/next-action.ts` from `T-007`, and the new API surface
  - five gates re-run: unit 36/36, isolation, typecheck 0 errors, build, integration 7/7, lint clean
  - verified the cycle live from an empty database rather than from the diff: both required-field
    refusals returned 400 and left **zero** projects behind, a full submission created project and
    action together, the row rendered the sentence unchanged, close-and-replace returned 201, the
    same close again 422, and the history kept the closed row. A project inserted without an action
    — the state `T-012` created — offers writing the first one
  - checked atomicity in the adapter rather than trusting the Outcome: `BEGIN IMMEDIATE`/`COMMIT`/
    `ROLLBACK` around both writes, with validation before the store is touched. Confirmed whitespace
    is trimmed at the API layer, so the rule storing untrimmed values is not reachable
  - mutation-probed the new rule: removing the empty-`trigger` check fails exactly one test
  - one Low — a project created past the cap lands `shelved` and still gets an action, which
    `createNextAction:83` refuses for non-active projects. Read `ProjectCard` to check the blast
    radius: shelved rows never render the cycle, so it is invisible. Rules disagree; behaviour fine
  - retrieved rather than recalled, since the machine jumped to Node `v26.8.1`: `node:sqlite` is
    still "Stability: 1.2 — Release candidate" on 26, so `D-019` stands. The pin gap is `D-013`'s
  - owner validated the cycle and raised where creation lives. Measured the panel with three
    projects — four forms, eighteen fields — and routed it to `T-014`. Fixed nothing here
