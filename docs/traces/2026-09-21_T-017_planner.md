## Trace

- 2026-09-21 — role: Planner
  - read: `D-024`, `research.md` §1 §3 §5 §6 §10 §12, `brief.md` § Users § Scope § Constraints,
    `requirements.json`, `user-stories.json`, `data-model.md` § NextAction and § Validation rules,
    `design-handoff.md` § The Project Row § The Log Form and the route table, `README.md`
  - did: `FR-6` rewritten from one if–then next action to a step list bounded to the next stretch;
    `FR-22` added for the day mark; `FR-20`'s estimate moved onto the step; `US-4` criterion and
    noun; `NextAction` replaced by `Step` with `markedFor`, `title`, `doneAt`, and the ER edge;
    the one-open-action invariant removed and two step rules put in its place; day rollover added
    to § Data lifecycle; the project row now renders today's steps as prose; the cycle form cut
    from four fields to one; the three guardrails written into `brief.md` § Constraints
  - files: `requirements.json`, `user-stories.json`, `data-model.md`, `design-handoff.md`,
    `brief.md`, `research.md`, `README.md` — no code, no migration
  - checks: baseline unit 45/45, isolation, typecheck 0 errors, build, integration 7/7, all green
    before any edit; final identical, since nothing under `core/`, `src/`, `adapters/` moved
  - scope added during the work: `research.md` §6's rule marked overridden and §12's noted as
    carried by `FR-22`. Finding, sources and effect size left verbatim — the evidence is not
    rewritten to match the product — but a rule left untouched would still instruct a reader to
    build the trigger
  - assumptions: the obstacle field leaves with the trigger, because §6 was its only source and
    `D-024` overrides §6. `D-024` does not say so itself; recorded in the `Step` table and here
  - result: the specification describes steps and a day list; the running code still asks for a
    trigger, and the manual says so. Open for owner validation; no blocker.
