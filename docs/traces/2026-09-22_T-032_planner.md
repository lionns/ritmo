## Trace

- 2026-09-22 — role: Planner
  - read: `data-model.md` § Commitment, Week, Tag, Entry and the weekly cycle; `requirements.json`
    `FR-7`…`FR-19` and `NFR-8`; `design-handoff.md` § Navigation Map and the close; `FR-15`'s
    implementation in `core/rules/project.ts:224`
  - found: **the task was over-scoped.** Three of its five "open" questions were settled in
    `data-model.md` on 2026-08-30 — a commitment hangs off a project, it is a frequency or a
    volume, and `proposedTarget` already sits beside `target` per week. I wrote the task without
    reading those sections closely, and put questions to the owner that the model had answered a
    month earlier. Their answers confirmed all three; no harm beyond their time.
  - found: `FR-15` needed no answer at all — it governs the active cap, which
    `Area.countsAgainstCap` already implements, not what a commitment hangs off
  - found, and this one is real: **`Commitment` had no `unit`.** `target` is an integer and
    nothing said whether it meant times or minutes, so neither the close nor `FR-10`'s proposal
    could read it. Added in `D-030`.
  - did: recorded `D-030` — Monday on the local calendar, the median of the last 4 closed weeks,
    a week that closes itself unlabelled, and `unit` — then brought `requirements.json` and
    `data-model.md` into line with it
  - labelled: the 4 is mine and has nothing behind it. `D-030` says so in bold, as
    `core/rules/calibration.ts` does for its own two constants.
  - checks: harness lint clean; no code touched, so the five gates are unchanged
  - open: nothing blocking T-033. The first weeks will propose nothing, by design.
  - result: closable.
