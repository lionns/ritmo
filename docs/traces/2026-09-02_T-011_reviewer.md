## Trace

- 2026-09-02 — role: Reviewer, round 1 on `T-011` (compressed)
  - read: the diff, `T-011`, `D-018`…`D-022`, the specs, the new adapter, the rewritten harness
  - reviewed the move from scratch, because the claim under test is that it runs without a vendor:
    deleted `data/` and `dist/`, then build, `db:reset`, `seed`, `npm start` — three routes at 200,
    two entries written over real HTTP, today's mark `height="104" data-state="timed"`
  - hard criterion confirmed by diff; probed the boundary against the *new* platform — `core/`
    importing `node:sqlite` is rejected, `adapters/` importing `node:fs` is allowed. Retrieved
    rather than recalled: `node:sqlite` is "Stability: 1.2 — Release candidate", RC since v24.15.0
  - one Medium, three Low, two nits. The Medium is an unscoped `scripts/` edit, mutation-probed both
    ways: real coupling still caught, so process defect rather than hole. Fixed nothing

- 2026-09-02 — role: Reviewer, round 2
  - five gates re-run: unit 29/29, isolation, typecheck 0 errors, build, integration 6/6, lint
    clean; rebuilt and re-probed the running server at 200 on all three routes
  - all three Low closed. Did not take the wiring fix on the trace's word: mutated
    `localDatabasePath()` to ignore `RITMO_DB_PATH`, confirmed the new test is the only one of six
    that fails, restored it. Then checked whether that run wrote into the owner's real
    `data/ritmo.sqlite` — it did not, but only because the fixture project is absent there, which
    is luck rather than a guard, and the nit says so
  - `scripts/` untouched as instructed. Owner approved that edit and asked for the record: wrote
    `D-023`, with no foundation marker so `boundaries` stays settled by `D-017` alone, and no
    `VERSION.md` entry — the same reading the owner settled in `T-009`, stated there rather than
    silently skipped. Deleted by id the two entries I had written into `data/ritmo.sqlite`
