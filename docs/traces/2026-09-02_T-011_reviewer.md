## Trace

- 2026-09-02 — role: Reviewer, round 1 on `T-011`
  - read: the diff, `T-011`, `D-018`…`D-022`, `architecture.md`, `quality-gates.md`, the new
    `adapters/sqlite/`, the rewritten integration harness, and `git show HEAD` for the old worker
  - five gates re-run here: unit 29/29, isolation, typecheck 0 errors over 32 files, build,
    integration 5/5, `harness-lint` clean at 22 decisions, one accepted per foundation topic
  - reviewed the move from scratch rather than from the diff, because the claim is that it runs
    without a vendor: deleted `data/` and `dist/`, then build, `db:reset`, `seed`, `npm start`.
    `/`, `/registrar` and `/api/portfolio` served 200; two entries written over real HTTP returned
    201; today's mark rendered `height="104" data-state="timed"`. No `wrangler` or `@cloudflare/*`
    in `node_modules`, no account, no `.wrangler`
  - hard criterion confirmed by diff: `core/`, `contracts/`, `src/components/`, `src/lib/` and
    `migrations/` unchanged. Probed the boundary against the *new* platform — `core/` importing
    `node:sqlite` is rejected, `adapters/` importing `node:fs` is allowed. Probes deleted
  - retrieved rather than recalled: `node:sqlite` is "Stability: 1.2 — Release candidate", RC since
    v24.15.0, which is what the implementer's assumption claimed. It checks out
  - one Medium, three Low, two nits. The Medium is an unscoped `scripts/` edit; mutation-probed both
    ways before calling it, and real coupling is still caught, so it is a process defect rather than
    a hole. Routed to the owner, not the implementer — second occurrence this session after `T-009`
  - fixed nothing. Task back to `doing`
