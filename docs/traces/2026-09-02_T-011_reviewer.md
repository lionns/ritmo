## Trace

- 2026-09-02 — role: Reviewer, round 1 on `T-011` (compressed)
  - reviewed the move from scratch, because the claim under test is that it runs without a vendor:
    deleted `data/` and `dist/`, then build, `db:reset`, `seed`, `npm start` — three routes at 200,
    entries written over real HTTP, today's mark `height="104" data-state="timed"`
  - hard criterion confirmed by diff; probed the boundary against the *new* platform — `core/`
    importing `node:sqlite` is rejected. Retrieved rather than recalled: `node:sqlite` is
    "Stability: 1.2 — Release candidate", RC since v24.15.0
  - one Medium, three Low, two nits; the Medium an unscoped `scripts/` edit, mutation-probed both
    ways: real coupling still caught, so process defect rather than hole. Fixed nothing

- 2026-09-02 — role: Reviewer, round 2
  - five gates re-run green, integration 6/6; rebuilt and re-probed the server at 200 on all routes
  - all three Low closed. Did not take the wiring fix on the trace's word: mutated
    `localDatabasePath()` to ignore `RITMO_DB_PATH`, confirmed the new test is the only one of six
    that fails, restored it. Checked whether that run wrote into the owner's real file — it did not,
    but only because the fixture project is absent there, which is luck, and the nit says so
  - `scripts/` untouched as instructed. Owner approved that edit and asked for the record: wrote
    `D-023`, no foundation marker so `boundaries` stays settled by `D-017` alone, and no
    `VERSION.md` entry — the reading the owner settled in `T-009`, stated rather than skipped
  - closed on the owner's validation. The last criterion was earned rather than waived: `/registrar`
    serves the form and its inlined module carrying the chips and the `fetch`, and on a throwaway
    `RITMO_DB_PATH` database the mark went 12px `untimed` → 104px `timed`. The POST was HTTP, not a
    browser click; the Outcome says so. Owner's file never touched, verified after
