## Trace

- 2026-09-01 — role: Reviewer, `/code-review high` on the `T-007` consumption
  - read: the working-tree diff, then the whole branch the pass widened to; `T-007`'s criteria;
    `PageStage`/`HeroChart` for the tooltip claim; `core/rules/` for the creation gap
  - checks: unit 25/25, integration 4/4, isolation, typecheck, build, lint — run here
  - probes: reset and seed, then read the live API beside the rendered page — `progressSincePlan`
    3/3/0 against `recentEntries` 5/5/0, drawn as 3/3/0 marks. The change is right.
  - found: the round's own change is clean. Two Medium and one Low stand against earlier `T-006`
    work, the worst being that removing the chart legend was justified by `<title>` tooltips that
    `pointer-events-none` and `role="img"` both make unreachable — a rationale I wrote myself
  - rejected one finding: the seed's verification `SELECT` is `T-007`'s acceptance criterion, in
    those words, not debug residue
  - could not verify: the panel's fallback scroll is a layout claim and no browser is connected;
    folded into the owner's visual check rather than asserted
  - false alarm of my own, worth recording: a `500` from `/api/portfolio` was a stale dev server on
    another port pointing at a database I had just reset, not a defect
  - blocker: `T-006` stays `review`; the legend rationale needs the owner, not the reviewer
