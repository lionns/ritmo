## Trace

- 2026-09-01 — role: Reviewer, `/code-review high` on `t-006-project-row` (`351319f`)
  - read: branch diff against `main`, task file, handoff, `core/rules/portfolio.ts`, `hero-chart.ts`
  - checks: every gate green, so all nine findings are behavioral, none a broken gate
  - found: 9 — one Medium/High (`AC-5` reachable with no visible change), three Medium (marks decay
    against `NFR-7`; two sentence defects), five Low. Reproduced each against the source
  - did: fixed the unambiguous ones — terminal punctuation, leading `¿`, blank act, the weak
    unconditional-mark guard (mutation-tested), and a stale probe line in the implementer trace
  - then closed the three Low of the earlier round on the owner's word: dead `::-webkit-scrollbar`
    block removed, `scrollbar-gutter` dropped so a non-scrolling panel stays centred, and a veiled
    chart now hides its legend. The key was rebuilt as bars and the owner rejected it on sight —
    correct: reading the four artboards showed the header's right slot is a per-screen context
    label, which both pages duplicated as a body eyebrow. Key, figcaption and eyebrows removed;
    `AppShell` owns the slot. Handoff § The Hero Chart and § Navigation Map rewritten to match
  - owner then settled all three: the last movement returns as a `dim` line (the failing `AC-5` probe
    now passes — 201, then the line changed, in the same-day untimed case that produced nothing),
    the act joins verbatim so a proper noun survives, and the marks should count entries since the
    open next action opened — **blocked here**, needing `NextAction.createdAt` in the contract and a
    window wider than 28 days, so it is a finding against `T-005`
  - own defect, caught after landing in `aa17a7e`: the § The Hero Chart rewrite deleted
    § The Project Row from the handoff; no gate covers prose, restored from `HEAD~1`
  - **independence: none.** `agent-config.md` assigns Frontend Implementer to Codex and Reviewer to
    Claude, on work it did not write. Claude wrote this `src/` and reviewed it, owner-accepted on
    2026-09-01. This review carries the blind spots of its own author; weigh it accordingly.
