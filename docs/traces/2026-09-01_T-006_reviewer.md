## Trace

- 2026-09-01 — role: Reviewer, `/code-review high` on `t-006-project-row` (`351319f`)
  - read: branch diff against `main`, task file, `design-handoff.md` § The Project Row,
    `core/rules/portfolio.ts`, `hero-chart.ts`, `agent-config.md`
  - checks: `npm test` 22/22, `npm run check:core` clean, `npm run build` clean — every gate green,
    so all nine findings are behavioral, none is a broken gate
  - found: 9 — one Medium/High (`AC-5` reachable with no visible change), three Medium (marks decay
    with the 28-day window against `NFR-7`; two sentence-composition defects), five Low
  - probes: reproduced all four sentence cases directly against `src/lib/project-row.ts`; confirmed
    `UNTIMED_HEIGHT` is fixed, so a second untimed entry on one day moves no bar
  - did: fixed the unambiguous defects only — terminal punctuation, leading `¿`, blank act, the weak
    unconditional-mark guard (mutation-tested: reintroducing the conditional now fails 1/22), and a
    stale probe line in the implementer trace
  - left: findings 1, 2 and the proper-noun half of 3 need the owner; 5–7 belong to the earlier round
  - **independence: none.** `agent-config.md` assigns Frontend Implementer to Codex and Reviewer to
    Claude, on work it did not write. Claude wrote this `src/` and reviewed it, owner-accepted on
    2026-09-01. This review carries the blind spots of its own author; weigh it accordingly.
