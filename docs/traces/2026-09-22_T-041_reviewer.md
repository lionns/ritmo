## Trace

- 2026-09-22 — role: Reviewer, on work Codex wrote
  - read: the whole diff — `core/rules/{project,week}.ts`, `src/pages/api/projects.ts`, both
    test suites
  - re-ran: five gates — unit 67/67, isolation, typecheck 0 errors, build, integration 36/36,
    harness-lint clean
  - verified rather than accepted: `isWeekStart` across the whole of Monday (00:00:01 and
    23:59:59), on Sunday and Tuesday, and on the Monday after a DST change, in `America/Bogota`,
    `Europe/Madrid` and `Pacific/Kiritimati` — consistent in all of them
  - checked the criterion most likely to be faked: "is it Monday" delegates to `weekStartsOn`
    rather than a `getDay() === 1` living in `project.ts`, so the week has one definition
  - findings: none
  - not verified: the screen. `ProjectMenu.astro:125` has been promising "Lo activo se cambia los
    lunes" since T-030 and is true for the first time today; nobody has seen it be true.
  - result: approved and closed.
