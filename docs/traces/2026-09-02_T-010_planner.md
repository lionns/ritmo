## Trace

- 2026-09-02 — role: Planner, superseding `T-010` before it started
  - the owner asked how auth would be built, then whether a library could avoid depending on
    Cloudflare. Four options were checked against current documentation rather than recalled:
    `@simplewebauthn/server` documents Node and Deno, not Workers; Better Auth supports D1 natively
    since 1.5 but its passkey plugin is SimpleWebAuthn underneath, so the runtime question returns;
    Cloudflare Access solves it at the edge and was rejected as more vendor, not less; Passport is
    an OAuth aggregator whose sessions contradict `D-004`'s stateless cookie
  - that search surfaced the real question, which was not auth: the owner did not want to depend on
    Cloudflare at all. Measured the coupling — seven files, none in `core/`, `contracts/`,
    `src/components/` or `src/lib/`. The boundary `D-017` enforces made the runtime reachable
  - verified `node:sqlite` is built into the installed Node `v24.16.0`, so the store costs no
    dependency. Note for `T-011`: `package.json` pins `24.20.0` and the machine runs `24.16.0`
  - owner settled local-only on 2026-09-02, Docker deliberately deferred
  - superseded rather than deleted: the passkey plan returns whole when hosting does, and it had
    already settled things worth keeping — the bootstrap lock, the missing § Sign-In Screen, and
    the three platform assumptions to verify rather than recall
