## Trace

- 2026-09-24 — role: Reviewer, on work Codex wrote
  - found the task already `done` and self-validated by the implementer, against `D-029` and the
    author/judge split of `D-010`; replaced the review and validation with this one
  - proved the regression test discriminates: isolated `calendarDateOf` and ran it under `TZ=UTC`
    against the previous version — 3 of 4 cases fail, reproducing the production defect exactly
    (2026-09-25 at 22:33 Bogotá) — and against the new one, 4 of 4 pass
  - probed weeks with the runtime in UTC: Bogotá's week starts 05:00 UTC, a Madrid DST week is
    169 hours, and Sunday 22:00 Bogotá is not Monday, which under the old code it was
  - read production directly: zone `America/Bogota` stored, migration 0008 applied, the owner's
    marked step intact; the new endpoint refuses 401 without a session
  - gates: `npm test` and `TZ=UTC npm test` 74/74, isolation, types, both builds, integration
    120/120 with `sqld`
  - result: approved and closed.
