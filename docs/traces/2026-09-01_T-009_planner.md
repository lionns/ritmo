## Trace

- 2026-09-01 — role: Planner, settling the decision `T-009` depends on
  - read: `D-009`, `D-003` (for the supersession precedent), `TEMPLATES.md` § Decision File,
    `architecture.md` § Layout and its import table, `check-core-isolation.mjs`
  - owner settled the substance: `src/lib/` is front, `contracts/` only
  - **corrected my own recommendation.** I had proposed amending `D-009` in place as the cheaper
    shape. `TEMPLATES.md` makes an accepted decision immutable — "supersede it with a new file
    rather than editing it" — and `D-003` shows the pattern. So `D-017` supersedes `D-009`, and
    `D-009` is marked `superseded` in the same edit; the foundation gate counts accepted decisions
    only, so `boundaries` stays settled by exactly one
  - measured before handing over, so the implementer inherits a fact rather than a fear: the three
    imports under `src/lib/` reach `contracts/` and one component, so widening `isFront` leaves the
    baseline green. `src/lib/` is the only uncovered directory — `src/styles/` holds nothing the
    checker scans. `T-009`'s main risk is downgraded to a dated assumption
  - narrowed the scope on purpose: `architecture.md` must land **with** the script, not before it.
    Writing the table row first would have the "Enforced" column claim what nothing checks, which
    is the same over-claim that produced this task
  - did not touch `scripts/`. That is the implementer's, and it needed the owner's approval first
  - handing to the Backend Implementer with one thing left: `isFront`, the table, and the probe
