## Trace

- 2026-09-01 — role: Reviewer on `T-009`
  - read: the diff, `T-009`, `D-017`, `VERSION.md` § Versioning and § Change Rules, `T-004` § Scope,
    `architecture.md` § Layout and its table, the checker. Five gates re-run here, green
  - ran the task's own probe rather than trusting the trace, both directions: `src/lib/__probe.ts`
    importing an adapter now fails naming the path, `src/components/` still fails, removing both
    restores clean. The rule widened rather than being replaced, which was the stated condition.
    Probes deleted; tree clean afterwards
  - the implementation half is right and the fix is one word. The architecture table also gained
    `src/layouts/`, covered by the check since `T-001` and never listed — an old omission closed
  - Medium against the governance half: the change bumped `harness.json` to `0.8.2` and added a
    `VERSION.md` entry, both named in this task's own § Out of Scope, which was left standing while
    § Assumptions was rewritten around it. On the merits it is not harness behaviour —
    `check-core-isolation.mjs` implements `D-017`, this project's architecture — and `PATCH` is
    defined as restoring documented behaviour, where `src/lib/` being front was never documented.
    It also consumed the last two lines of the `docs/sdd/` budget
  - owner instructed the revert. Did it myself rather than routing another round: `harness.json`,
    `VERSION.md` and the front-matter back to `0.8.1`, budget back to 648/650. Left `scripts/` and
    `architecture.md` untouched — those are the implementer's and they are correct
  - did not touch the implementer's trace. It records what that round did; erasing it would falsify
    the record. The approval it claims is not visible in this repository and I did not resolve it
    by assumption
  - closed 2026-09-01 on the owner's validation, final checks re-run at close. Nothing in the
    product changed, so validation here is agreeing with the revert, not clicking a screen
