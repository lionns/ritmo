## Trace

- 2026-09-23 — role: Reviewer, on work Codex wrote
  - read: `src/middleware.ts`, `adapters/http/{auth,session,password}.ts`, the auth route, the
    new port and `docs/authentication.md`
  - re-ran every gate: unit 67/67, isolation, typecheck 0 errors, both builds, integration
    **120/120** with `sqld` installed, harness-lint clean
  - **enumerated rather than sampled**, against a running server: 22 paths with no cookie, with a
    forged cookie and with garbage. Every page 302 to `/entrar`, every API 401 — `/api/export`
    included. `grep getOnlyOwner src/` returns zero.
  - the check that mattered: `scrypt` from `node:crypto` and `AsyncLocalStorage` are not
    guaranteed on workerd, and the catch-all would have made a failure look like a wrong password
    — the owner locked out of their own deployment with no way to tell why. Signed in on the
    **workerd build** with the real password: 200, session minted, every screen served.
  - also verified live: one byte flipped in the signature → 401; a foreign `Origin` on a write →
    403; rotating `RITMO_SESSION_SECRET` → old cookies 401/302; logout → 200 and cookie cleared
  - my own false alarm: logout first returned 403, because I requested `127.0.0.1` while sending
    `Origin: localhost`. Astro's own CSRF check, my test's fault, not the product's.
  - noted without a task: logout does not invalidate the token itself, only the browser's copy —
    `D-004`'s recorded consequence, bounded by the 30-minute lifetime; and the catch-all at
    `auth.ts:129` records nothing anywhere
  - left open, correctly: registering a passkey on a second device. The tests use synthetic
    credentials; `T-040` carries the real-phone check.
  - cleaned up: both servers stopped, `.dev.vars` removed, owner's database untouched
  - result: approved and closed.
