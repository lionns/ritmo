## Trace

- 2026-09-24 — role: Reviewer, on work Codex wrote — rounds 1–3, compressed
  - round 1 · tested the **live deployment** over the network rather than reading the trace: 12
    paths with no cookie and with a forged one — every page 302 to `/entrar`, every API 401,
    `/api/export` included; `no-store`, `same-origin` and `X-Frame-Options: DENY` present. One
    request returned `000`; retried three times and it is 401 every time — a blip, not a hole.
  - round 1 · **found High**: `http://.../entrar` answered **200** over plain HTTP with the
    password field on it, `http://.../` redirected without upgrading the scheme, and no
    `Strict-Transport-Security` was sent — a password typed there crosses the wire in cleartext
    before the origin check rejects the POST. Returned.
  - round 1 · local source untouched, 1/4/4/8/6 rows. Could not verify the hosted row counts, the
    hosted export or the rollback rehearsal: all need the owner's credentials, which a reviewer
    must not hold. `NFR-1`, unmet since `D-020`, is met on the owner's report.
  - round 2 · the fix worked in production and killed `npm run dev`: every `http:` request
    redirected, including `http://localhost`, to an `https://localhost:PORT` with no TLS listener.
    That breaks this task's own criterion, so the box was unchecked and it went back a second time
    with the rule to mirror named — `session.ts:78`.
  - round 3 · `npm run dev` works again over plain `http://localhost`: `/` → 302 to `/entrar`,
    `/entrar` → 200, no upgrade attempted. Production, redeployed as `e822deb6`, still 308s to
    `https://` with HSTS and still refuses everything without a session.
  - round 3 · gates in full: unit 67/67, isolation, types, both builds, integration **120/120**
    against a real libSQL server, lint clean. Recorded without returning: "is this local" now
    exists twice, here and in `session.ts:78`.
  - result: approved, not closed — the phone passkey and a second device are the owner's to do.
