## Trace

- 2026-09-24 — role: Reviewer, on work Codex wrote
  - tested the **live deployment** over the network rather than reading the trace: 12 paths with no
    cookie and with a forged cookie — every page 302 to `/entrar`, every API 401, `/api/export`
    included; `no-store`, `same-origin` and `X-Frame-Options: DENY` all present
  - one result came back `000` on a forged-cookie request; retried three times and it is 401 every
    time, so a transient network blip, not a hole
  - **found High**: `http://ritmo.juan-account.workers.dev/entrar` answers **200** over plain HTTP
    with the password field on it, and `http://.../` redirects to `http://.../entrar` — the scheme
    is never upgraded, and no `Strict-Transport-Security` header is sent. A password typed there
    crosses the wire in cleartext before the origin check rejects the POST. Returned.
  - checked the local source database: untouched, 1/4/4/8/6 rows, integrity ok
  - could not verify: row counts on the hosted store, the hosted export, the rollback rehearsal.
    All three need the owner's credentials, which a reviewer must not hold. Stated rather than
    implied.
  - `NFR-1`, unmet and recorded as unmet since `D-020` on 2026-09-02, is met on the owner's report
    of under five seconds from their phone.
  - result: returned, one round — HTTPS enforcement and HSTS.
