## Trace

- 2026-09-24 — role: Reviewer, on work Codex wrote
  - tested the **live deployment** over the network rather than reading the trace: 12 paths with no
    cookie and with a forged one — every page 302 to `/entrar`, every API 401, `/api/export`
    included; `no-store`, `same-origin` and `X-Frame-Options: DENY` present. One request returned
    `000`; retried three times and it is 401 every time — a network blip, not a hole.
  - **found High**: `http://.../entrar` answered **200** over plain HTTP with the password field on
    it, `http://.../` redirected to `http://.../entrar` without upgrading the scheme, and no
    `Strict-Transport-Security` was sent. A password typed there crosses the wire in cleartext
    before the origin check rejects the POST. Returned.
  - local source database untouched: 1/4/4/8/6 rows, integrity ok. Could not verify the hosted row
    counts, the hosted export or the rollback rehearsal — all need the owner's credentials, which a
    reviewer must not hold. `NFR-1`, unmet since `D-020`, is met on the owner's report.

- 2026-09-24 — role: Reviewer — round 2
  - the returned finding is fixed in production, checked live: `http://.../entrar` → **308** to
    `https://`, `strict-transport-security: max-age=31536000` present, auth surface unchanged
  - tested the regression risk first, and it is real: `npm run dev` now answers **308** to
    `https://localhost:PORT`, which has no TLS listener, so the connection fails outright. The
    middleware redirects every `http:` request without the localhost exemption that
    `session.ts:78` already defines one file away.
  - that breaks this task's own criterion — "still runs `npm run dev` and the whole suite
    unchanged" — so the box is unchecked again and the task returned a second time
  - result: returned. One line, and the rule to mirror is named.
