## Trace

- 2026-09-22 — role: Reviewer, on work Codex wrote
  - read: `core/rules/{week,commitment}.ts` in full, the port and adapter diffs,
    `migrations/0006_commitment_unit.sql`, `core/rules/project.ts:175`,
    `ProjectMenu.astro:118-127`, and `data-model.md` § Derived values
  - re-ran: five gates — unit 63/63, isolation, typecheck 0 errors (3 pre-existing hints), build,
    integration 36/36, harness-lint clean
  - verified rather than accepted: `weekStartsOn` at both week edges, across New Year, a leap day,
    `America/Bogota` and `Europe/Madrid`; a DST week's bounds measure 169 hours, so the "calendar
    arithmetic, not seven 24-hour periods" comment is true; `weeks` carries the
    `UNIQUE (owner_id, starts_on)` the upsert depends on; the live database holds zero weeks and
    zero commitments, so `hasClosedWeek` is still false for the owner
  - verified the migration's claim directly: `ALTER TABLE ... NOT NULL` with no default succeeds on
    an empty table and is refused on a populated one — "fail rather than guess a unit" holds
  - confirmed both of the implementer's own findings, and both are against the spec, not the code:
    `FR-14`'s Monday has never been implemented while the screen already promises it, and
    `data-model.md:222` contradicts `D-030` §2 on the proposal
  - acted: opened `T-041` for the Monday exception (`D-029` §2 — `T-035` owns the message, not the
    rule); marked the `FR-10` conflict in place as unimplementable until the owner chooses
  - noted: nothing outside `core/` calls the new rules. By design — `T-034` wires them.
  - result: approved and closed; the implementer had held it open for a browser check a model-only
    task does not need.
